import { describe, expect, it } from "vitest";
import { applyRegionalRules, demoAnalysis } from "../lib/analysis";
import { AnalysisSchema, parseAIAnalysis } from "../lib/schema";
describe("분석 및 지역 규칙",()=>{ it("데모 응답은 스키마를 만족한다",()=>expect(AnalysisSchema.parse(demoAnalysis("pet")).overallConfidence).toBe(.92)); it("폐건전지는 별도 수거 안내를 받는다",()=>expect(applyRegionalRules(demoAnalysis("battery"),"sample-seoul").disposalGuides[0].collectionType).toContain("전용")); it("서울 자치구에는 서울시 시행 안내 출처를 연결한다",()=>expect(applyRegionalRules(demoAnalysis("pet"),"seoul-gangnam").region.regionalRuleVerified).toBe(true)); it("낮은 신뢰도는 추가 확인을 요청한다",()=>expect(demoAnalysis("unknown").needsMoreInformation).toBe(true)); it("AI의 단일 문자열 배열 응답을 안전하게 정규화한다",()=>{const parsed=parseAIAnalysis({detectedItems:{name:"페트병",category:"플라스틱",material:"PET",confidence:"0.9",recyclableStatus:"RECYCLABLE",contamination:{status:"NONE",description:"깨끗함"},components:[],specialWaste:false,evidence:"PET 표시",uncertainties:[]},needsMoreInformation:false,followUpQuestions:[],warnings:[],overallConfidence:"0.9"});expect(parsed.detectedItems[0].material).toEqual(["PET"]);}); });
